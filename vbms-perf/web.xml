<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://java.sun.com/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_2_5.xsd"
         version="2.5">

	<display-name>vbms-cms-web</display-name>

	<!-- Location of parent context shared by all wars in the ear -->
	<context-param>
		<param-name>locatorFactorySelector</param-name>
		<param-value>classpath:/META-INF/spring/common-parent-beanRefContext.xml</param-value>
	</context-param>

	<!-- Parent context shared by all wars in the ear -->
	<context-param>
		<param-name>parentContextKey</param-name>
		<param-value>vbms.ear.context</param-value>
	</context-param>

	<!-- The definition of the Root Spring Container shared by all Servlets and Filters -->
	<context-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>classpath:/META-INF/spring/cms-web-root-context.xml</param-value>
	</context-param>
	<context-param>
		<param-name>log4jExposeWebAppRoot</param-name>
		<param-value>false</param-value>
	</context-param>

	<!-- Creates the Spring Container shared by all Servlets and Filters -->
	<listener>
		<listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
	</listener>

	<listener>
		<listener-class>org.springframework.web.context.request.RequestContextListener</listener-class>
	</listener>

	<listener>
		<listener-class>org.springframework.web.util.Log4jConfigListener</listener-class>
	</listener>

    <servlet>
        <servlet-name>CXFServlet</servlet-name>
        <servlet-class>
            org.apache.cxf.transport.servlet.CXFServlet
        </servlet-class>
        <!-- get config from web application context to save memory since we using same spring context-->
        <!--<init-param>-->
            <!--<param-name>config-location</param-name>-->
            <!--<param-value>classpath:/META-INF/spring/cms-web-root-context.xml</param-value>-->
        <!--</init-param>-->
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>CXFServlet</servlet-name>
        <url-pattern>/streaming/*</url-pattern>
    </servlet-mapping>

    <filter>
            <display-name>PerformanceLoggingFilter</display-name>
            <filter-name>PerformanceLoggingFilter</filter-name>
            <filter-class>gov.va.vba.vbms.common.framework.performance.PerformanceLoggingFilter</filter-class>
    </filter>
    <filter>
        <filter-name>springSecurityFilterChain</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>

    <filter-mapping>
            <filter-name>PerformanceLoggingFilter</filter-name>
            <url-pattern>/*</url-pattern>
    </filter-mapping>
    <filter-mapping>
        <filter-name>springSecurityFilterChain</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

</web-app>